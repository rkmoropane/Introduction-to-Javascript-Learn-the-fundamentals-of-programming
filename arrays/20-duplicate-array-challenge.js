
// Original array
const originalArray = [1, 2, 3, 4, 5];

// Duplicate the original array using the spread operator
const newArray = [...originalArray];
// Print the duplicate array
console.log(newArray);