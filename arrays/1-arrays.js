// An array is a data structure that contains a group of elements. 
// Typically these elements are all of the same data type, such as
// an integer or string 
// Arrays are commonly used in computer programs to organize data
// so that a related set of values can be 
// easily sorted or searched 

// Create an Array
// Access an Array item using the index position 

const fruits = ["Apple", "Pineapple", "Watermelon", "Pear"];
fruits.push("Orange");
fruits.push("Grapes");
console.log(fruits);
fruits.pop();
console.log(fruits);


// run the pop function 
let orange = fruits.pop();
console.log(fruits);
